import '../styles/Loading.scss'
import loading from '../icon/logo.png'

export const Loading =()=>{
    return(
        <>
            <img className='loading' src={loading} alt="" />
        </>
    )
}